# 1. OX 문제 [총 10점, 정답 +2점, 오답 -2점, 공백 0점]
# (a) 산점도를 통해 두 변수 간의 직선의 관계 뿐만 아니라, 곡선의 관계도 알 수 있다. (  O   )
# (b) 상관계수를 통해 두 변수간의 인과 관계를 알 수 있다. (  X   )
# (c) X ~ binomial(n,p)를 따르는 확률변수 X에 대하여,
#     n은 충분히 크고, p는 충분히 작으면 포아송 분포로 근사할 수 있다. ( O    )
# (d) pnorm(x, lower.tail = F)는 표준정규분포를 따르는 확률변수 X에 대하여 P[X <= x]를 계산한다. (  X   )
# (e) 정규확률그림이 직선에 가까우면 모집단이 정규분포를 따른다고 할 수 있다. (  O   )
